{
  "name": "firebase-demo",
  "private": true,
  "version": "0.0.0",
  "description": "A firebase demo",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "serve": "./node_modules/http-server/bin/http-server -a localhost -p 8000 -c-1 ./src"
  }
}
